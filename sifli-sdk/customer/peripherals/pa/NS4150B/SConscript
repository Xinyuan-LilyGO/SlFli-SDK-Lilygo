from building import *

cwd = GetCurrentDir()
src = Glob('*.c')
CPPPATH = [cwd + '/../common', cwd]
group = DefineGroup('Drivers', src, depend = ['PA_USING_NS4150B'], CPPPATH = CPPPATH)

Return('group')
